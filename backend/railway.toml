[build]
# Пересобирать и передеплоивать бэкенд-сервис на Railway только если
# реально изменились файлы внутри backend/. Без этого любой пуш в
# репозиторий (даже правки только в desktop-app/ или frontend/)
# запускал бы новый деплой и вставал в очередь на бесплатном плане.
watchPatterns = ["**"]
